name: Pylint

on: [push]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pylint
        pip install "numpy<2" torch torchvision --index-url https://download.pytorch.org/whl/cpu
        pip install -r requirements-r3.txt
        pip install --no-deps -r requirements-r3-source.txt
    - name: Test model storage and geometry adapters without CUDA or model downloads
      run: python -m unittest discover -s tests -v
    - name: Analysing the code with pylint
      run: |
        pylint --errors-only \
          --disable=import-error,no-member,not-callable,\
        possibly-used-before-assignment,access-member-before-definition,\
        no-name-in-module,undefined-variable \
          $(git ls-files '*.py')
